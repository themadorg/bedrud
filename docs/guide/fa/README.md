# راهنماهای عملیاتی

نکات کاربردیِ استقرار که مکمل مستندات عمومی سایت هستند.

| راهنما | زمان استفاده |
|---|---|
| [nginx + Let's Encrypt در جلوی بدرود](./nginx-letsencrypt-behind-proxy.md) | خاتمه TLS در لبه شبکه (Edge)؛ بدرود فقط روی HTTP باقی می‌ماند؛ سیگنالینگ LiveKit از `wss://` روی پورت ۴۴۳ استفاده می‌کند. |

**مستندات عمومی مرتبط:** [پشت پروکسی/CDN](https://bedrud.org/en/docs/guides/behind-proxy) · [نصب و راه‌اندازی](https://bedrud.org/en/docs/getting-started/installation)
