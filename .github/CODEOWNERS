# The master ruleset has require_code_owner_review enabled, so this file is enforced.
#
# Covers the whole .github/ directory, not just workflows/, on purpose: this file itself
# lives under .github/, so a narrower rule lets it be edited — a reviewer quietly removed,
# say — with one ordinary approval and no code owner involved.
#
# All three have admin on this repository. Carlos-Adams is a code owner of .github/ in
# bedrud-android but is deliberately absent here: they have read access only, and GitHub
# ignores code owners without write access, so listing them would look like coverage that
# does not exist.
/.github/ @omidz4t @narseh-ir @mamaru-a
