# Requires "Require review from Code Owners" enabled on the master ruleset to take effect.
#
# Covers the whole .github/ directory, not just workflows/, on purpose: this file itself
# lives under .github/, so a narrower rule lets it be edited — a reviewer quietly removed,
# say — with one ordinary approval and no code owner involved.
#
# Carlos-Adams is a code owner of .github/ in bedrud-android but is deliberately absent
# here: they have read access to this repository, and GitHub ignores code owners without
# write access, so listing them would look like coverage that does not exist.
/.github/ @omidz4t @narseh-ir
